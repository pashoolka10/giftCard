# GameCard — магазин пополнения игровых аккаунтов (лабораторные работы N1–N3)

Учебный статический сайт магазина пополнения игровых аккаунтов:
карты PlayStation Store и Apple Gift Card, пополнение Steam по логину,
игры для PS5 (например, GTA VI).

## Как открыть

Открыть файл `index.html` в браузере (двойным щелчком). Сайт полностью статический,
сервер и база данных не нужны — данные каталога лежат в `js/data.js`.

## Структура файлов (требование лабораторной работы N2)

```
web/
├── index.html      — главная страница
├── catalog.html    — каталог товаров с «умным фильтром» и формой пополнения Steam
├── cart.html       — корзина и оформление заказа
├── profile.html    — профиль (раздел в разработке)
├── admin.html      — админ панель (раздел в разработке)
├── about.html      — о нас, контакты, карта
├── styles.css      — основные стили сайта
├── print.css       — стили версии для печати
├── images/         — графические файлы (логотип, карты пополнения)
└── js/
    ├── data.js     — данные товаров (массив products)
    ├── catalog.js  — вывод каталога, «умный фильтр» и форма Steam
    ├── cart.js     — корзина на localStorage
    └── main.js     — переключение версии для печати, год в подвале
```

## Что реализовано по лабораторным работам

**Лабораторная работа N1** (мини-сайт): несколько связанных страниц с общим макетом
(шапка, нижнее меню, подвал), каталог товаров с фотографиями и описанием,
страница «О нас» с показателями и контактами, карта проезда
(виджет Яндекс.Карт с адресом БГТУ).

**Лабораторная работа N2** (семантическая вёрстка): блочная вёрстка на `div` и
семантических тегах (`header`, `main`, `section`, `article`, `aside`, `footer`, `address`),
все стили вынесены в `styles.css`, названия классов и файлов — на английском,
у всех картинок заполнен `alt`, меню оформлены списками `ul`/`li`,
разные состояния ссылок (обычная, при наведении, посещённая, активная) отличаются,
кодировка UTF-8.

**Лабораторная работа N3** (динамическая страница):
- «Умный фильтр» на странице `catalog.html` без перезагрузки страницы:
  платформа, страна, номинал, цена. Значения, которые приведут к пустому результату,
  становятся недоступными (зачёркнуты и заблокированы).
- Критериев фильтрации — 4 (по заданию нужно не меньше 3).
- Данные хранятся в JS-массиве (`js/data.js`) — вариант 2 из методических указаний.
- Версия для печати: кнопка «Версия для печати» переключает стилевой файл
  (`print.css`) без перезагрузки страницы, меню, фильтр и оформление скрываются,
  на экране остаётся только содержимое страницы.

## Пополнение Steam

У Steam нет карт пополнения: платформа есть в фильтре, но при её выборе вместо
карточек появляется форма, где нужно указать логин аккаунта и сумму пополнения
в рублях. Остальные фильтры (страна, номинал) для Steam недоступны — так работает
«умный фильтр».

## Пример работы фильтра

1. В фильтре выбрать платформу «PlayStation» — страны «США» и «Канада» станут недоступными.
2. Выбрать номинал «1000 INR» — в каталоге останется карта «PlayStation Store Индия 1000 INR».
3. Выбрать платформу «Steam» — появятся поля «Логин Steam» и «Сумма пополнения, ₽».

## Что можно улучшить дальше

- регистрация и авторизация пользователей (в профиле указано, что раздел скоро появится);
- наполнение админ панели: управление товарами, заказами и пользователями;
- настоящая оплата и автоматическая выдача кодов.
